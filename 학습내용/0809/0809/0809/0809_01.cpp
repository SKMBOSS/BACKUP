/*
#include <iostream>
#include <cmath>

using namespace std;

//두점의 거리를 구하는 함수 모든점은 +값
float getDistance(int x1, int y1, int x2, int y2)
{
	return  sqrtf(powf(x1 - x2, 2) + pow(y1 - y2, 2));
}

struct AAA
{
	int a;
	int b;
	char szName[256];
};

int main()
{
	//cout << sqrtf(4) << endl;
	//cout << getDistance(1, 1, 2, 1)<<endl;

	AAA a;

	a.a = 10;
	a.b = 20;

	memset(&a, 0, sizeof(a));
	cout << a.a << endl;

	//memcpy();

	system("pause");
	return 0;
}
*/